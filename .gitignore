# dependencies
node_modules
.pnp
.pnp.js

# build
dist
dist-ssr
*.local

# editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# logs
logs
*.log
npm-debug.log*

# env
.env
.env.*
!.env.example

# scratch / reference clones
.scratch
.scratch_dev.log
nodefield-ref
.playwright-mcp
phase*.png

# build/QA review screenshots (scratch)
s0-*.png
s1-*.png
s1a-*.png
s3-*.png
s4-*.png
s6-*.png
m-morph.png
m-machine.png
final-*.png
new-*.png
morph-fallback.png
skel-*.png
lazy-morph.png
promise-fixed.png
glow-*.png
neural-*.png
closer-*.png
skel-check.png
morph-skeleton.png

# raw source media — reference only, not needed by the build (keeps the
# iteration repo lean; the cut clips in public/ are what the site uses)
announce*.mp4
announce*.m4a
frames/
contact_sheets/
inspo_mov/

# build info + root scratch (screenshots, plans) — not part of the app
*.tsbuildinfo
/*.png
/PLAN_*.md

# stray scratch (not part of the app)
experiments/
/*.jpeg
/*.jpg
hero-*.jpeg

# local agent/editor config
.claude/

# tsc -b artifacts for the node config (source of truth is vite.config.ts)
vite.config.js
vite.config.d.ts
